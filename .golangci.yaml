linters-settings:
  errcheck:
    check-type-assertions: true
    check-blank: true
  govet:
    enable-all: true
  varnamelen:
    ignore-names:
      - tc # https://dave.cheney.net/2019/05/07/prefer-table-driven-tests

linters:
  enable:
    - errcheck # default
    - gosimple # default
    - govet # default
    - ineffassign # default
    - staticcheck # default
    - unused # default
    - funlen
    - depguard
    - errname
    - exhaustive
    - gomnd
    - gosec
    - ireturn
    - lll
    - nilerr
    - unparam
    - whitespace
    - wrapcheck
    - varnamelen
    - wsl
